services:
  rss-reader:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - rss-data:/app/data
    restart: unless-stopped
    environment:
      - RSS_SERVER_PORT=3000

volumes:
  rss-data:
